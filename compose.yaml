services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lab5-server
    ports:
      - "1234:1234/udp"
    volumes:
      - ./data.csv:/app/data.csv
    restart: unless-stopped
